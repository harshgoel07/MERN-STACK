{
  "name": "mern-stack-project",
  "version": "1.0.0",
  "description": "This is a simple login and registration project built with React (frontend) and Node.js (backend). The project uses Vite for the frontend build tool and MongoDB as the database. For authentication, bcrypt is used to hash passwords and JWT tokens are used for user sessions.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd client && npm run dev\" \"cd server && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "moment": "^2.30.1"
  }
}
